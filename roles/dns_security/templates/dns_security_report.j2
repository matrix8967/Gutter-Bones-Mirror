<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DNS Security Testing Report - {{ dns_security_summary.hostname }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            text-align: center;
        }

        .header h1 {
            color: #2c3e50;
            font-size: 2.5em;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .header .subtitle {
            color: #7f8c8d;
            font-size: 1.2em;
            margin-bottom: 20px;
        }

        .header .meta-info {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 20px;
        }

        .meta-item {
            text-align: center;
        }

        .meta-item .label {
            font-weight: bold;
            color: #34495e;
            display: block;
            margin-bottom: 5px;
        }

        .meta-item .value {
            color: #2980b9;
            font-size: 1.1em;
            font-weight: 600;
        }

        .dashboard {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .metric-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .metric-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 35px rgba(0, 0, 0, 0.25);
        }

        .metric-card .icon {
            font-size: 3em;
            margin-bottom: 15px;
        }

        .metric-card .value {
            font-size: 2.2em;
            font-weight: bold;
            margin-bottom: 8px;
        }

        .metric-card .label {
            color: #7f8c8d;
            font-size: 0.95em;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .status-excellent { color: #27ae60; }
        .status-good { color: #2980b9; }
        .status-warning { color: #f39c12; }
        .status-critical { color: #e74c3c; }

        .bg-excellent { background: linear-gradient(135deg, #a8e6cf, #88d8a3); }
        .bg-good { background: linear-gradient(135deg, #74b9ff, #0984e3); }
        .bg-warning { background: linear-gradient(135deg, #fdcb6e, #e17055); }
        .bg-critical { background: linear-gradient(135deg, #fd79a8, #e84393); }

        .section {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 25px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        .section h2 {
            color: #2c3e50;
            font-size: 1.8em;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 3px solid #3498db;
        }

        .section h3 {
            color: #34495e;
            font-size: 1.4em;
            margin-bottom: 15px;
        }

        .test-results {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .test-item {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 15px;
            border-left: 5px solid #3498db;
        }

        .test-item.pass {
            border-left-color: #27ae60;
            background: linear-gradient(135deg, #d5f4e6, #e8f8f0);
        }

        .test-item.fail {
            border-left-color: #e74c3c;
            background: linear-gradient(135deg, #ffeaa7, #fab1a0);
        }

        .test-item.warning {
            border-left-color: #f39c12;
            background: linear-gradient(135deg, #ffeaa7, #fdcb6e);
        }

        .test-name {
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 5px;
        }

        .test-result {
            color: #7f8c8d;
            font-size: 0.9em;
        }

        .data-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .data-table thead {
            background: #34495e;
            color: white;
        }

        .data-table th,
        .data-table td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        .data-table tbody tr:hover {
            background: #f5f5f5;
        }

        .data-table tbody tr:nth-child(even) {
            background: #f8f9fa;
        }

        .alert {
            padding: 15px 20px;
            border-radius: 8px;
            margin-bottom: 15px;
            border-left: 5px solid;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border-left-color: #27ae60;
        }

        .alert-warning {
            background: #fff3cd;
            color: #856404;
            border-left-color: #f39c12;
        }

        .alert-danger {
            background: #f8d7da;
            color: #721c24;
            border-left-color: #e74c3c;
        }

        .progress-bar {
            width: 100%;
            height: 20px;
            background: #ecf0f1;
            border-radius: 10px;
            overflow: hidden;
            margin: 10px 0;
        }

        .progress-fill {
            height: 100%;
            transition: width 0.5s ease;
            border-radius: 10px;
        }

        .progress-excellent { background: #27ae60; }
        .progress-good { background: #2980b9; }
        .progress-warning { background: #f39c12; }
        .progress-critical { background: #e74c3c; }

        .footer {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            color: #7f8c8d;
            margin-top: 30px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        .collapsible {
            cursor: pointer;
            padding: 10px;
            background: #3498db;
            color: white;
            border: none;
            border-radius: 5px;
            margin-bottom: 10px;
            width: 100%;
            text-align: left;
            font-size: 1em;
        }

        .collapsible:hover {
            background: #2980b9;
        }

        .collapsible-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease;
        }

        .collapsible-content.active {
            max-height: 1000px;
        }

        .badge {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 15px;
            font-size: 0.8em;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .badge-success { background: #27ae60; color: white; }
        .badge-warning { background: #f39c12; color: white; }
        .badge-danger { background: #e74c3c; color: white; }
        .badge-info { background: #3498db; color: white; }

        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }

            .header h1 {
                font-size: 2em;
            }

            .dashboard {
                grid-template-columns: 1fr;
            }

            .test-results {
                grid-template-columns: 1fr;
            }

            .header .meta-info {
                flex-direction: column;
                gap: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header Section -->
        <div class="header">
            <h1>🛡️ DNS Security Testing Report</h1>
            <div class="subtitle">Comprehensive DNS Infrastructure Security Analysis</div>

            <div class="meta-info">
                <div class="meta-item">
                    <span class="label">Target Host</span>
                    <span class="value">{{ dns_security_summary.hostname }}</span>
                </div>
                <div class="meta-item">
                    <span class="label">Test Session</span>
                    <span class="value">{{ dns_security_summary.session_id }}</span>
                </div>
                <div class="meta-item">
                    <span class="label">Timestamp</span>
                    <span class="value">{{ dns_security_summary.timestamp }}</span>
                </div>
                <div class="meta-item">
                    <span class="label">Duration</span>
                    <span class="value">{{ dns_security_summary.test_duration }}s</span>
                </div>
            </div>
        </div>

        <!-- Dashboard Metrics -->
        <div class="dashboard">
            {% set dashboard_success_rate = dns_security_summary.baseline_health.success_rate | default(0) | int %}
            <div class="metric-card {% if dashboard_success_rate >= 95 %}bg-excellent{% elif dashboard_success_rate >= 90 %}bg-good{% elif dashboard_success_rate >= 80 %}bg-warning{% else %}bg-critical{% endif %}">
                <div class="icon">🎯</div>
                <div class="value {% if dashboard_success_rate >= 95 %}status-excellent{% elif dashboard_success_rate >= 90 %}status-good{% elif dashboard_success_rate >= 80 %}status-warning{% else %}status-critical{% endif %}">
                    {{ dashboard_success_rate }}%
                </div>
                <div class="label">DNS Success Rate</div>
            </div>

            {% set dashboard_blocking_rate = dns_security_summary.security_findings.malicious_blocking_rate | default(0) | int %}
            <div class="metric-card {% if dashboard_blocking_rate >= 95 %}bg-excellent{% elif dashboard_blocking_rate >= 85 %}bg-good{% elif dashboard_blocking_rate >= 70 %}bg-warning{% else %}bg-critical{% endif %}">
                <div class="icon">🛡️</div>
                <div class="value {% if dashboard_blocking_rate >= 95 %}status-excellent{% elif dashboard_blocking_rate >= 85 %}status-good{% elif dashboard_blocking_rate >= 70 %}status-warning{% else %}status-critical{% endif %}">
                    {{ dashboard_blocking_rate }}%
                </div>
                <div class="label">Threat Blocking</div>
            </div>

            {% set dashboard_total_queries = dns_security_summary.performance_metrics.total_queries | default(0) | int %}
            {% set dashboard_successful_queries = dns_security_summary.performance_metrics.successful_queries | default(0) | int %}
            {% set dashboard_perf_rate = (dashboard_successful_queries * 100 / dashboard_total_queries) | round(0) | int if dashboard_total_queries > 0 else 0 %}
            <div class="metric-card {% if dashboard_perf_rate >= 90 %}bg-excellent{% elif dashboard_perf_rate >= 80 %}bg-good{% elif dashboard_perf_rate >= 70 %}bg-warning{% else %}bg-critical{% endif %}">
                <div class="icon">⚡</div>
                <div class="value {% if dashboard_perf_rate >= 90 %}status-excellent{% elif dashboard_perf_rate >= 80 %}status-good{% elif dashboard_perf_rate >= 70 %}status-warning{% else %}status-critical{% endif %}">
                    {{ dashboard_perf_rate }}%
                </div>
                <div class="label">Performance</div>
            </div>

            <div class="metric-card {% if not dns_security_summary.security_findings.https_interception %}bg-excellent{% else %}bg-critical{% endif %}">
                <div class="icon">🔒</div>
                <div class="value {% if not dns_security_summary.security_findings.https_interception %}status-excellent{% else %}status-critical{% endif %}">
                    {% if dns_security_summary.security_findings.https_interception %}DETECTED{% else %}CLEAR{% endif %}
                </div>
                <div class="label">HTTPS Interception</div>
            </div>
        </div>

        <!-- DNS Baseline Health -->
        <div class="section">
            <h2>📊 DNS Baseline Health</h2>

            {% set success_rate = dns_security_summary.baseline_health.success_rate | default(0) | int %}
            {% if success_rate >= 90 %}
            <div class="alert alert-success">
                <strong>✅ Excellent DNS Health:</strong> {{ success_rate }}% success rate indicates robust DNS infrastructure.
            </div>
            {% elif success_rate >= 80 %}
            <div class="alert alert-warning">
                <strong>⚠️ Acceptable DNS Health:</strong> {{ success_rate }}% success rate has room for improvement.
            </div>
            {% else %}
            <div class="alert alert-danger">
                <strong>🚨 Poor DNS Health:</strong> {{ success_rate }}% success rate indicates significant DNS issues.
            </div>
            {% endif %}

            <div class="test-results">
                {% set total_queries = dns_security_summary.baseline_health.total_queries | default(0) | int %}
                {% set failed_queries = dns_security_summary.baseline_health.failed_queries | default(0) | int %}
                <div class="test-item {% if success_rate >= 90 %}pass{% elif success_rate >= 80 %}warning{% else %}fail{% endif %}">
                    <div class="test-name">DNS Resolution Success Rate</div>
                    <div class="test-result">{{ success_rate }}% ({{ total_queries - failed_queries }}/{{ total_queries }} queries successful)</div>
                    <div class="progress-bar">
                        <div class="progress-fill {% if success_rate >= 90 %}progress-excellent{% elif success_rate >= 80 %}progress-warning{% else %}progress-critical{% endif %}" style="width: {{ success_rate }}%"></div>
                    </div>
                </div>

                <div class="test-item {% if failed_queries == 0 %}pass{% else %}fail{% endif %}">
                    <div class="test-name">Failed Query Count</div>
                    <div class="test-result">{{ failed_queries }} failed queries out of {{ total_queries }} total</div>
                </div>
            </div>
        </div>

        <!-- Security Findings -->
        <div class="section">
            <h2>🔍 Security Analysis</h2>

            <h3>DNS Poisoning Detection</h3>
            {% set poisoning_incidents = dns_security_summary.security_findings.dns_poisoning_incidents | default(0) | int %}
            {% if poisoning_incidents == 0 %}
            <div class="alert alert-success">
                <strong>✅ No DNS Poisoning Detected:</strong> All DNS resolvers returned consistent responses.
            </div>
            {% else %}
            <div class="alert alert-danger">
                <strong>🚨 Potential DNS Poisoning:</strong> {{ poisoning_incidents }} potential incidents detected with inconsistent responses.
            </div>
            {% endif %}

            <div class="test-results">
                <div class="test-item {% if poisoning_incidents == 0 %}pass{% else %}fail{% endif %}">
                    <div class="test-name">DNS Consistency Check</div>
                    <div class="test-result">{% if poisoning_incidents == 0 %}All resolvers consistent{% else %}{{ poisoning_incidents }} inconsistencies found{% endif %}</div>
                </div>
            </div>

            <h3>Malicious Domain Blocking</h3>
            {% set blocking_rate = dns_security_summary.security_findings.malicious_blocking_rate | default(0) | int %}
            {% if blocking_rate >= 85 %}
            <div class="alert alert-success">
                <strong>✅ Excellent Threat Protection:</strong> {{ blocking_rate }}% of malicious domains are being blocked.
            </div>
            {% elif blocking_rate >= 70 %}
            <div class="alert alert-warning">
                <strong>⚠️ Moderate Threat Protection:</strong> {{ blocking_rate }}% blocking rate could be improved.
            </div>
            {% else %}
            <div class="alert alert-danger">
                <strong>🚨 Poor Threat Protection:</strong> Only {{ blocking_rate }}% blocking rate - significant security risk.
            </div>
            {% endif %}

            <div class="test-results">
                <div class="test-item {% if blocking_rate >= 85 %}pass{% elif blocking_rate >= 70 %}warning{% else %}fail{% endif %}">
                    <div class="test-name">Malicious Domain Blocking</div>
                    <div class="test-result">{{ blocking_rate }}% effectiveness</div>
                    <div class="progress-bar">
                        <div class="progress-fill {% if blocking_rate >= 85 %}progress-excellent{% elif blocking_rate >= 70 %}progress-good{% else %}progress-critical{% endif %}" style="width: {{ blocking_rate }}%"></div>
                    </div>
                </div>
            </div>

            <h3>HTTPS Security</h3>
            <div class="test-results">
                <div class="test-item {% if not dns_security_summary.security_findings.https_interception %}pass{% else %}fail{% endif %}">
                    <div class="test-name">HTTPS Interception</div>
                    <div class="test-result">{% if dns_security_summary.security_findings.https_interception %}🚨 DETECTED - Certificates may be intercepted{% else %}✅ NOT DETECTED - HTTPS connections appear secure{% endif %}</div>
                </div>

                <div class="test-item {% if not dns_security_summary.security_findings.captive_portal %}pass{% else %}warning{% endif %}">
                    <div class="test-name">Captive Portal</div>
                    <div class="test-result">{% if dns_security_summary.security_findings.captive_portal %}⚠️ PRESENT - Network may require authentication{% else %}✅ NOT PRESENT - Direct internet access{% endif %}</div>
                </div>
            </div>
        </div>

        <!-- Performance Metrics -->
        <div class="section">
            <h2>⚡ Performance Analysis</h2>

            {% set total_perf_queries = dns_security_summary.performance_metrics.total_queries | default(0) | int %}
            {% set successful_perf_queries = dns_security_summary.performance_metrics.successful_queries | default(0) | int %}
            {% if total_perf_queries > 0 and successful_perf_queries >= total_perf_queries * 0.9 %}
            <div class="alert alert-success">
                <strong>✅ Excellent Performance:</strong> {{ successful_perf_queries }}/{{ total_perf_queries }} queries successful ({{ ((successful_perf_queries / total_perf_queries) * 100) | round(1) }}%).
            </div>
            {% elif total_perf_queries > 0 and successful_perf_queries >= total_perf_queries * 0.7 %}
            <div class="alert alert-warning">
                <strong>⚠️ Good Performance:</strong> {{ successful_perf_queries }}/{{ total_perf_queries }} queries successful ({{ ((successful_perf_queries / total_perf_queries) * 100) | round(1) }}%).
            </div>
            {% else %}
            <div class="alert alert-danger">
                <strong>🚨 Poor Performance:</strong> Only {{ successful_perf_queries }}/{{ total_perf_queries }} queries successful.
            </div>
            {% endif %}

            <div class="test-results">
                <div class="test-item {% if total_perf_queries > 0 and successful_perf_queries >= total_perf_queries * 0.9 %}pass{% elif total_perf_queries > 0 and successful_perf_queries >= total_perf_queries * 0.7 %}warning{% else %}fail{% endif %}">
                    <div class="test-name">Query Success Rate</div>
                    <div class="test-result">{{ successful_perf_queries }}/{{ total_perf_queries }} successful</div>
                </div>
            </div>
        </div>

        <!-- Control D Integration -->
        {% if dns_controld_integration %}
        <div class="section">
            <h2>🎛️ Control D Integration Status</h2>

            {% if dns_security_summary.controld_integration.service_accessible and dns_security_summary.controld_integration.ctrld_active %}
            <div class="alert alert-success">
                <strong>✅ Control D Fully Operational:</strong> Service is accessible and ctrld daemon is active.
            </div>
            {% elif dns_security_summary.controld_integration.service_accessible %}
            <div class="alert alert-warning">
                <strong>⚠️ Control D Partially Operational:</strong> Service accessible but local daemon may not be running.
            </div>
            {% else %}
            <div class="alert alert-danger">
                <strong>🚨 Control D Issues:</strong> Service appears to be inaccessible or misconfigured.
            </div>
            {% endif %}

            <div class="test-results">
                <div class="test-item {% if dns_security_summary.controld_integration.service_accessible %}pass{% else %}fail{% endif %}">
                    <div class="test-name">verify.controld.com Accessibility</div>
                    <div class="test-result">{% if dns_security_summary.controld_integration.service_accessible %}✅ Accessible{% else %}❌ Not accessible{% endif %}</div>
                </div>

                <div class="test-item {% if dns_security_summary.controld_integration.ctrld_active %}pass{% else %}fail{% endif %}">
                    <div class="test-name">Local ctrld Service</div>
                    <div class="test-result">{% if dns_security_summary.controld_integration.ctrld_active %}✅ Active{% else %}❌ Inactive{% endif %}</div>
                </div>

                <div class="test-item">
                    <div class="test-name">DNS Server</div>
                    <div class="test-result">{{ dns_security_summary.controld_integration.dns_server }}</div>
                </div>
            </div>
        </div>
        {% endif %}

        <!-- Advanced Details (Collapsible) -->
        <div class="section">
            <button class="collapsible" onclick="toggleCollapsible(this)">🔧 Technical Details & Raw Data</button>
            <div class="collapsible-content">
                <h3>Test Configuration</h3>
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Parameter</th>
                            <th>Value</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>Session ID</td>
                            <td>{{ dns_security_summary.session_id }}</td>
                        </tr>
                        <tr>
                            <td>Target Hostname</td>
                            <td>{{ dns_security_summary.hostname }}</td>
                        </tr>
                        <tr>
                            <td>Test Duration</td>
                            <td>{{ dns_security_summary.test_duration }} seconds</td>
                        </tr>
                        <tr>
                            <td>Timestamp</td>
                            <td>{{ dns_security_summary.timestamp }}</td>
                        </tr>
                    </tbody>
                </table>

                {% if dns_baseline_test is defined and dns_baseline_test.stdout_lines %}
                <h3>DNS Baseline Results</h3>
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Status</th>
                            <th>Resolver</th>
                            <th>Domain</th>
                            <th>Response Time (ms)</th>
                            <th>Result</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for result in dns_baseline_test.stdout_lines %}
                        {% set result_parts = result.split(',') %}
                        <tr>
                            <td><span class="badge {% if result_parts[0] == 'PASS' %}badge-success{% else %}badge-danger{% endif %}">{{ result_parts[0] }}</span></td>
                            <td>{{ result_parts[1] }}</td>
                            <td>{{ result_parts[2] }}</td>
                            <td>{{ result_parts[3] }}</td>
                            <td>{{ result_parts[4] }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
                {% endif %}

                {% if malicious_domain_test is defined and malicious_domain_test.stdout_lines %}
                <h3>Malicious Domain Test Results</h3>
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Domain</th>
                            <th>Resolver</th>
                            <th>Response</th>
                            <th>Blocked</th>
                            <th>Category</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for result in malicious_domain_test.stdout_lines %}
                        {% set result_parts = result.split(',') %}
                        <tr>
                            <td>{{ result_parts[0] }}</td>
                            <td>{{ result_parts[1] }}</td>
                            <td>{{ result_parts[2] }}</td>
                            <td><span class="badge {% if result_parts[3] == 'true' %}badge-success{% else %}badge-danger{% endif %}">{{ result_parts[3] }}</span></td>
                            <td>{{ result_parts[4] }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
                {% endif %}
            </div>
        </div>

        <!-- Recommendations -->
        <div class="section">
            <h2>💡 Recommendations</h2>

            <div class="test-results">
                {% if success_rate < 90 %}
                <div class="test-item warning">
                    <div class="test-name">Improve DNS Reliability</div>
                    <div class="test-result">Consider configuring additional DNS resolvers or investigating network connectivity issues</div>
                </div>
                {% endif %}

                {% if blocking_rate < 85 %}
                <div class="test-item warning">
                    <div class="test-name">Enhance Threat Protection</div>
                    <div class="test-result">Configure DNS filtering service or update blocklists to improve malicious domain blocking</div>
                </div>
                {% endif %}

                {% if total_perf_queries > 0 and (successful_perf_queries / total_perf_queries) < 0.8 %}
                <div class="test-item warning">
                    <div class="test-name">Optimize DNS Performance</div>
                    <div class="test-result">Consider using closer DNS servers or implementing DNS caching to improve response times</div>
                </div>
                {% endif %}

                {% if not dns_security_summary.controld_integration.ctrld_active %}
                <div class="test-item warning">
                    <div class="test-name">Enable Control D</div>
                    <div class="test-result">Consider setting up Control D for enhanced DNS filtering and security</div>
                </div>
                {% endif %}
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>Generated by Gutter Bonez DNS Security Testing Framework</p>
            <p>Report generated at {{ dns_security_summary.timestamp }}</p>
            <p>🦴 Nowhere to go but up. Always lots left to do.</p>
        </div>
    </div>

    <script>
        function toggleCollapsible(element) {
            const content = element.nextElementSibling;
            if (content.classList.contains('active')) {
                content.classList.remove('active');
                element.textContent = element.textContent.replace('▼', '▶');
            } else {
                content.classList.add('active');
                element.textContent = element.textContent.replace('▶', '▼');
            }
        }

        // Initialize collapsible buttons
        document.addEventListener('DOMContentLoaded', function() {
            const collapsibles = document.querySelectorAll('.collapsible');
            collapsibles.forEach(function(collapsible) {
                if (!collapsible.textContent.includes('▶') && !collapsible.textContent.includes('▼')) {
                    collapsible.textContent = '▶ ' + collapsible.textContent;
                }
            });
        });
    </script>
</body>
</html>
